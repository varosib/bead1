<h1>Listázás</h1>
<div class="panel">
  <form method="get" action="/list" >
    <div class="form-group">
      <div class="col-lg-6 col-lg-offset-2">
        <input type="date" class="form-control" 
               name="query" id="search" 
               {{#if query}}
               value="{{query}}"
               {{/if}}
               placeholder="Írja be, hogy mire keres">
      </div>
      <div class="col-lg-2">
        <input type="submit" class="btn btn-primary" value="Keresés">
      </div>
    </div>
  </form>
</div>
<table class="table table-striped table-hover ">
  <thead>
    <tr>
      <th>Étel</th>
      <th>Hozzávaló</th>
      <th>Felhasználó</th>
    </tr>
  </thead>
  <tbody>
    {{#each data as |recept|}}
    <tr>
      <td>{{recept.etel}}</td>
      <td>{{recept.hozzavalo}}</td>
      <td>{{recept.felhasznalonev}}</td>
      <td>
        <a href="/edit/{{recept.id}}">Szerkesztés</a>
      </td>  
      <td>
        <a href="/delete/{{recept.id}}">Törlés</a>
      </td>
    </tr>
    {{/each}}
  </tbody>
</table> 